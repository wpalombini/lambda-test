# AWS LAMBDA AND LOGGING (CLOUDWATCH) TEST

## This is a tiny experiment showing how to:

- Package a lambda function

- Deploy it to AWS

- Generate different types (INFO, WARN, ERROR) types of logging in CloudWatch
